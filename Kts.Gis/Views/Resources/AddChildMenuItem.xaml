<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Меню добавления дочернего объекта -->
    <Style
        x:Key="AddChildMenuItemStyle"
        TargetType="{x:Type MenuItem}">
        <Setter
            Property="Command"
            Value="{Binding AddChildCommand}" />
        <Setter
            Property="Header"
            Value="{Binding Name}" />
    </Style>
</ResourceDictionary>