<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:views="clr-namespace:Kts.Gis.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Стиль изменялки вершины многоугольника -->
    <Style
        TargetType="{x:Type views:PolygonVertexThumb}">
        <Setter
            Property="Template">
            <Setter.Value>
                <ControlTemplate
                    TargetType="{x:Type views:PolygonVertexThumb}">
                    <Ellipse
                        x:Name="Ellipse"
                        Cursor="{TemplateBinding Cursor}"
                        Fill="White"
                        Height="{TemplateBinding Height}"
                        Stroke="Black"
                        Width="{TemplateBinding Width}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>